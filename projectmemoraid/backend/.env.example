# Resend API Configuration
# Get your API key from https://resend.com
RESEND_API_KEY=re_your_api_key_here

# Email Configuration
DEFAULT_FROM_EMAIL=onboarding@resend.dev

# Database Configuration (local MySQL — ignored on Render when DATABASE_URL is set)
DB_NAME=memoraid_db
DB_USER=root
DB_PASSWORD=root123
DB_HOST=127.0.0.1
DB_PORT=3306

# --- Production (Render) ---
# DATABASE_URL is auto-provided by Render when you add a PostgreSQL database.
# SECRET_KEY=generate-a-strong-random-key
# DEBUG=False
# ALLOWED_HOSTS=your-app.onrender.com
# CORS_ALLOWED_ORIGINS=https://your-app.vercel.app
